// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  baseUrlAPI: 'https://api.themoviedb.org',
};

// API Rick & Morty : https://rickandmortyapi.com/api/character
// 
// TheMovieDB       : https://api.themoviedb.org/3
// API_KEY          : 457643c75832b5d6f6c66e10f260618b
// LIST ID          : 7113210
// https://api.themoviedb.org/4/list/{list_id}?page=1&api_key=<<api_key>>

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
